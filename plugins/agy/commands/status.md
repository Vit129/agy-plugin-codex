---
description: Show active and recent agy jobs for this repository
argument-hint: '[job-id] [--wait] [--timeout-ms <ms>] [--all]'
---

Run the companion from the installed plugin root:

```bash
node "<plugin-root>/scripts/agy-companion.mjs" status $ARGUMENTS
```

For a job id, present full output. Without a job id, keep the status compact.
